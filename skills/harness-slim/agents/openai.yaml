interface:
  display_name: "Harness Slim"
  short_description: "Scaffold and audit coding-agent harnesses"
  default_prompt: "Use $harness-slim to create, validate, and benchmark a production-ready coding-agent harness for this repository."

policy:
  allow_implicit_invocation: true
