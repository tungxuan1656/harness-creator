interface:
  display_name: "Harness Router"
  short_description: "Route repository harness adoption phases"
  default_prompt: "Use $harness-router to audit repository harness capabilities and route only the phases it needs."
